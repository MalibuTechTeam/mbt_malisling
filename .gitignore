web/node_modules/
web/dist/
.agents/
.claude/
skills-lock.json

# Strategy / planning docs — live in the Obsidian vault, NOT in this public repo.
# feature_shooting.md especially documents the PAID/escrowed companion; keeping
# it (and the funnel strategy) out of the open-source repo is the whole point of
# the opaque bridge.
CLAUDE.md
feature_malisling.md
feature_shooting.md
roadmap.md
roadmap_malisling.md

# Local deploy helper — hardcodes the developer's own server path, so it is useless
# to anyone else and leaks the machine layout. install_ox_patch.ps1 reads it when
# present but falls back cleanly without it.
deploy-to-server.ps1

# Dev-only design mockups (scratch)
web/mockup-*.html

# Rescue / audit scratch files (prompts + raw reports)
.codex_*

# Audit outputs — canonical design docs live in the Obsidian vault, not the public repo
specs/
